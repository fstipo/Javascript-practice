'use strict';

// Document load and than window load
document.addEventListener('DOMContentLoaded', () => {
  console.log('Document is loaded');
  console.log('Document is loaded');
  console.log('Document is loaded');
});

window.addEventListener('DOMContentLoaded', () => {
  console.log('Window is loaded');
});
