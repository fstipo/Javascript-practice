'use strict';

const arr = ['a', 'b', 'c', 'd', 'e'];
arr;
arr.push('tom');
arr.unshift('Betty');
arr.pop();
arr.shift();
arr.indexOf('e');
arr.length;
arr.includes('5');
arr.includes('d');

console.log(arr);
